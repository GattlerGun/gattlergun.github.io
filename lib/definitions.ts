export type User = {
    id: string;
    name: string;
    email: string;
    password: string;
}

export type Project = {
    id: string;
    title: string;
    description: string;
    img: string;
    pageurl: string;
    giturl: string;
}